package com.global.picalc.core;

/**
 * Interface for setup/start calculator
 *
 * startCalculator() for calculating and get a result
 */
public interface Calculator {

	/**
	 * startCalculator() to start the calculating and get the result
	 */
    void startCalculator();
}
